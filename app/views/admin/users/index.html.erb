<h1><%= t('.title') %></h1>

<table>
  <thead>
    <tr>
      <th><%= Task.human_attribute_name(:name) %></th>
      <th><%= Task.human_attribute_name(:email) %></th>
      <th><%= "管理者権限" %></th>
      <th><%= "登録タスク数" %></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <%# <td><%= link_to t('common.show'), admin_user_path, class: "show-user" %></td>
        <%# <td><%= link_to t('common.edit'), edit_admin_user_path(user), class: "edit-user" %></td>
        <%# <td><%= link_to t('common.destroy'), admin_users_path(user), method: :delete, data: { confirm: t('common.confirm_destroy') }, class: "destroy-user"  %></td> 
      </tr>
    <% end %>
  </tbody>
</table>